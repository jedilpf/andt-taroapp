# 后端开发工程师面试试卷答案

**未来申活（上海）数字科技有限公司**

---

## 一、选择题答案（每题3分）

| 题号 | 答案 | 解析 |
|-----|------|------|
| 1 | C | static是修饰符，不是访问修饰符 |
| 2 | B | Spring Boot默认使用Logback作为日志实现 |
| 3 | B | InnoDB支持事务和外键，MyISAM不支持 |
| 4 | A | Redis默认使用0号数据库 |
| 5 | B | 404表示请求的资源不存在 |

---

## 二、填空题答案（每空2分）

| 题号 | 答案 |
|-----|------|
| 6 | 匿名内部类（或函数式接口实现） |
| 7 | 构造器、Setter |
| 8 | B+、O(log n) |
| 9 | Hash、ZSet |
| 10 | 一致性、持久性 |

---

## 三、简答题答案（每题8分）

**11. 什么是Spring的依赖注入？它有什么优点？**

评分要点：
- 定义：对象不自己创建依赖，由容器注入（3分）
- 优点：降低耦合、便于测试、便于维护（5分）

**12. 如何优化MySQL慢查询？**

评分要点：
- 使用EXPLAIN分析执行计划（2分）
- 检查索引使用情况（2分）
- 避免SELECT *、优化WHERE条件（2分）
- 合理使用索引、分页优化（2分）

**13. Redis有哪些使用场景？请列举至少3个。**

评分要点：
- 缓存（3分）
- 会话存储（2分）
- 分布式锁（2分）
- 计数器/排行榜（1分）

**14. 什么是JWT？它的工作原理是什么？**

评分要点：
- 定义：JSON Web Token，用于认证的令牌（3分）
- 组成：Header.Payload.Signature（2分）
- 工作原理：登录生成token、请求携带token、服务端验证（3分）

**15. 如何保证接口的幂等性？**

评分要点：
- 使用唯一标识（订单号、token）（3分）
- 数据库唯一索引（2分）
- Redis分布式锁（2分）
- 状态机校验（1分）

---

## 四、编程题答案

**16. SQL查询题（10分）**

```sql
SELECT 
    u.id,
    u.name,
    COUNT(o.id) as order_count,
    IFNULL(SUM(o.amount), 0) as total_amount
FROM user u
LEFT JOIN `order` o ON u.id = o.user_id
GROUP BY u.id, u.name;
```

评分标准：
- LEFT JOIN正确（3分）
- COUNT和SUM函数正确（4分）
- GROUP BY正确（3分）

**17. 单例模式（15分）**

```java
public class Singleton {
    private static volatile Singleton instance;

    private Singleton() {}

    public static Singleton getInstance() {
        if (instance == null) {
            synchronized (Singleton.class) {
                if (instance == null) {
                    instance = new Singleton();
                }
            }
        }
        return instance;
    }
}
```

评分标准：
- 私有构造函数（3分）
- volatile关键字（4分）
- 双重检查锁（5分）
- 线程安全（3分）

---

## 五、AI工具应用题答案

**18. 用户实体类提示词示例**

```
请帮我生成一个MyBatis Plus的用户实体类，要求：
1. 类名：User，包名：com.example.entity
2. 字段：id（主键）、phone（手机号）、password（密码）、nickname（昵称）
3. 使用Lombok注解：@Data、@Builder
4. 字段添加数据库注解和中文注释
```

---

**未来申活（上海）数字科技有限公司**
**编制日期：2026年4月8日**