# 地图模块总体概览

## 一、模块概述

| 属性 | 内容 
|------|------
| 模块名称 | 地图模块 
| 模块代码 | map 
| Phase-1优先级 | **P0（核心）** 
| 技术方案 | React-Leaflet + 高德地图瓦片 
| 核心价值 | 电工定位展示、地址选择、位置服

---

## 二、使用场景分

### 2.1 地图找电工（核心场景```
用户场景：用户需要快速找到附近的电工师傅

用户流程首页 点击"地图找电地图页面 查看附近电工 点击电工标记 查看详情 预约服务

核心功能├── 地图展示
  ├── 用户当前位置标记
  ├── 电工位置标记（价格气泡）
  └── 地图缩放/拖动
├── 电工筛├── 搜索（名字、技能）
  ├── 距离排序
  └── 状态筛选（空闲/忙碌├── 电工详情
  ├── 基本信息
  ├── 评分/订单├── 擅长标签
  └── 距离/起步└── 预约流程
    ├── 选择服务类型
    ├── 选择地址
    ├── 选择时间
    └── 提交预约
```

### 2.2 地址选择（辅助场景）

```
用户场景：用户需要选择或编辑收货地址/服务地址

使用位置├── 订单发布 选择服务地址
├── 地址管理 新增/编辑地址
├── 外卖配选择配送地址
└── 便民服务 选择服务地址

核心功能├── 地图选点
  ├── 拖动地图选择位置
  ├── POI搜索
  └── 附近地址推荐
├── 地址搜索
  ├── 关键词搜├── 历史记录
  └── 热门地点
└── 地址详情
    ├── 详细地址输入
    ├── 门牌└── 联系人信```

---

## 三、技术架

### 3.1 当前实现方案

```
技术栈├── 前端框架：React + TypeScript
├── 地图库：react-leaflet (v4.x)
├── 地图瓦片：高德地图瓦片服├── 状态管理：React Context (AppContext)
└── UI组件：Tailwind CSS + Lucide Icons

关键依赖├── react-leaflet：React封装的Leaflet地图├── leaflet：开源地图库
└── @types/leaflet：TypeScript类型定义
```

### 3.2 高德瓦片URL

```typescript
// 当前使用的高德地图瓦片URL
const AMAP_TILE_URL = "https://wprd04.is.autonavi.com/appmaptilelang=zh_cn&size=1&style=7&x={x}&y={y}&z={z}";

// URL参数说明// - lang=zh_cn：中文语言
// - size=1：标准尺// - style=7：标准地图样// - x, y, z：瓦片坐标（由Leaflet自动填充```

### 3.3 核心组件结构

```
地图模块组件结构├── MapPage.tsx（地图找电工主页面）
  ├── MapContainer（地图容器）
  ├── TileLayer（瓦片图层）
  ├── Marker（用户位置标记）
  ├── Marker[]（电工位置标记）
  ├── MapUpdater（地图视图更新）
  ├── ElectricianDetailModal（电工详情弹窗）
  ├── BookingDrawer（预约抽屉）
  └── AddressSelector（地址选择器）
└── UserAddressEdit.tsx（地址编辑页面├── MapPickerModal（地图选点弹窗├── MapContainer（地图容器）
    ├── TileLayer（瓦片图层）
    └── MapEvents（地图事件处理）
    └── AddressForm（地址表单```

---

## 四、数据结构设

### 4.1 位置数据结构

```typescript
// 位置信息（types.tsinterface Location {
  lat: number;      // 纬度
  lng: number;      // 经度
  address: string;  // 地址描述
}

// 电工数据（MapPage.tsxinterface ElecData {
  id: number;
  name: string;
  avatar: string;
  rating: string;
  orders: string;
  tags: string[];
  distance: string;
  price: number;
  priceStr: string;
  status: 'Busy' | 'Idle';
  lat: number;
  lng: number;
}

// 地址数据
interface Address {
  id: string;
  tag: string;          // 标签：家/公司
  address: string;      // 详细地址
  name: string;         // 联系phone: string;        // 电话
  lat: number;
  lng: number;
  detail: string;      // 门牌isDefault: boolean;  // 是否默认
}
```

### 4.2 地图配置

```typescript
// 默认坐标（上海市徐汇区）
const DEFAULT_LAT = 31.1940;
const DEFAULT_LNG = 121.4360;

// 地图配置
const MAP_CONFIG = {
  center: [DEFAULT_LAT, DEFAULT_LNG] as [number, number],
  zoom: 14,              // 默认缩放级别
  minZoom: 10,           // 最小缩maxZoom: 18,           // 最大缩zoomControl: false,    // 禁用默认缩放控件
};

// 坐标校验函数
function isValidLatLng(lat: any, lng: any): boolean {
  const nLat = Number(lat);
  const nLng = Number(lng);
  return typeof nLat === 'number' && !isNaN(nLat) && isFinite(nLat) &&
         typeof nLng === 'number' && !isNaN(nLng) && isFinite(nLng);
}
```

---

## 五、核心功能实

### 5.1 地图初始```typescript
// 地图容器组件
<MapContainer 
  center={safeCenter} 
  zoom={14} 
  style={{ height: '100%', width: '100%', zIndex: 0 }} 
  zoomControl={false} 
  ref={setMapInstance}
>
  <MapUpdater center={center} />
  <TileLayer 
    attribution='&copy; <a href="https://www.amap.com/">高德地图</a>' 
    url="https://wprd04.is.autonavi.com/appmaptilelang=zh_cn&size=1&style=7&x={x}&y={y}&z={z}" 
  />
  <Marker position={safeCenter} icon={userIcon} />
  {filteredElectricians.map((elec) => (
    <Marker 
      key={elec.id} 
      position={[Number(elec.lat), Number(elec.lng)]} 
      icon={createPriceMarkerIcon(elec, focusedElecId === elec.id)} 
      eventHandlers={{ click: () => handleMarkerClick(elec) }} 
    />
  ))}
</MapContainer>
```

### 5.2 地图视图更新

```typescript
// 地图视图更新组件
const MapUpdater = ({ center }: { center: { lat: number; lng: number } }) => {
  const map = useMap();
  
  useEffect(() => {
    if (center && isValidLatLng(center.lat, center.lng)) {
      try {
        map.flyTo([Number(center.lat), Number(center.lng)], 14, {
          animate: true,
          duration: 0.8,
        });
      } catch (e) {
        console.error("Map flyTo failed:", e);
      }
    }
  }, [center.lat, center.lng, map]);

  return null;
};
```

### 5.3 自定义标记图```typescript
// 用户位置标记
const userIcon = new DivIcon({
  className: 'user-location-marker',
  html: `
    <div class="relative flex items-center justify-center w-12 h-12">
      <div class="absolute w-full h-full bg-blue-500 opacity-20 rounded-full animate-ping"></div>
      <div class="w-4 h-4 bg-blue-600 rounded-full border-2 border-white shadow-md relative z-10"></div>
    </div>
  `,
  iconSize: [48, 48],
  iconAnchor: [24, 24],
});

// 电工价格标记
const createPriceMarkerIcon = (elec: ElecData, isSelected: boolean) => {
  const size = isSelected 56 : 48;
  return new DivIcon({
    className: 'custom-price-marker',
    html: `
      <div class="relative flex flex-col items-center transition-all duration-300 ${isSelected 'scale-110 z-50' : 'scale-100'}">
        <div class="bg-red-500 text-white text-[10px] font-bold px-2 py-0.5 rounded-full shadow-md mb-1 whitespace-nowrap border border-white">
          ${elec.priceStr}
        </div>
        <div class="relative">
          <img src="${elec.avatar}" class="w-12 h-12 rounded-full border-2 ${isSelected 'border-green-500' : 'border-white'} shadow-lg bg-gray-100 object-cover" />
          ${elec.status === 'Idle' '<div class="absolute bottom-0 right-0 w-3.5 h-3.5 bg-green-500 rounded-full border-2 border-white"></div>' : ''}
        </div>
      </div>
    `,
    iconSize: [size, size + 20],
    iconAnchor: [size / 2, size + 10],
  });
};
```

### 5.4 距离计算

```typescript
// 经纬度距离计算（Haversine公式function getDistanceFromLatLonInKm(
  lat1: any,
  lon1: any,
  lat2: any,
  lon2: any,
) {
  if (!isValidLatLng(lat1, lon1) | !isValidLatLng(lat2, lon2)) return '0.0km';
  
  const R = 6371; // 地球半径（公里）
  const dLat = deg2rad(Number(lat2) - Number(lat1));
  const dLon = deg2rad(Number(lon2) - Number(lon1));
  const a =
    Math.sin(dLat / 2) * Math.sin(dLat / 2) +
    Math.cos(deg2rad(Number(lat1))) *
      Math.cos(deg2rad(Number(lat2))) *
      Math.sin(dLon / 2) *
      Math.sin(dLon / 2);
  const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
  const d = R * c;
  return isNaN(d) '0.0km' : d.toFixed(1) + 'km';
}

function deg2rad(deg: number) {
  return deg * (Math.PI / 180);
}
```

---

## 六、地图事件处

### 6.1 地图拖动事件

```typescript
// 地址选择页面的地图事件处const MapEvents = () => {
  useMapEvents({
    dragend: (e) => {
      setSearching(true);
      // 根据新坐标反查地址
      setTimeout(() => {
        const randomIdx = Math.floor(Math.random() * mockNearby.length);
        setCenterAddr(mockNearby[randomIdx].name);
        setSearching(false);
      }, 600);
    },
  });
  return null;
};
```

### 6.2 标记点击事件

```typescript
// 电工标记点击处理
const handleMarkerClick = (elec: ElecData) => {
  if (!isValidLatLng(elec.lat, elec.lng)) return;
  
  setFocusedElecId(elec.id);
  
  // 地图飞到电工位置
  if (mapInstance) {
    mapInstance.flyTo([Number(elec.lat), Number(elec.lng)], 15);
  }
  
  // 滚动到对应卡const card = document.getElementById(`elec-card-${elec.id}`);
  if (card && listRef.current) {
    const container = listRef.current;
    const scrollLeft = card.offsetLeft - container.offsetLeft - container.clientWidth / 2 + card.clientWidth / 2;
    container.scrollTo({ left: scrollLeft, behavior: 'smooth' });
  }
};
```

---

## 七、高德地图API集成规划

### 7.1 当前方案 vs 完整API方案

| 功能 | 当前方案 | 高德API方案 
| 属性 | 接口概览 | 接口名称 |------| 属性 | 接口概览 | 接口名称 |----------| 属性 | 接口概览 | 接口名称 |------| 属性 | 接口概览 | 接口名称 |-------
| 地图展示 | 瓦片服务 | JS API 
| 定位服务 | 模拟 | 定位API 
| 地址搜索 | 模拟 | POI搜索API 
| 逆地理编| 模拟 | 地理编码API 
| 路线规划 | | 路径规划API 
| 导航功能 | | 导航API 

### 7.2 高德API Key申请

```
申请流程1. 访问高德开放平台：https://lbs.amap.com/
2. 注册开发者账3. 创建应用（Web小程序）
4. 获取API Key

Key类型├── Web端Key：用于Web应用
├── Web服务Key：用于服务端API调用
└── 小程序Key：用于微信小程序
```

### 7.3 需要集成的API

```
Phase-1 必需API├── JS API 2.0：地图展├── 定位API：获取用户位├── POI搜索API：地址搜索
└── 地理编码API：坐标与地址互转

Phase-2 扩展API├── 路径规划API：路线规├── 导航API：实时导└── 行政区域API：区域数```

---

## 八、模块交互关```
地图模块交互关系图：

┌─────────────────────────────────────────────────────────────用户端应├─────────────────────────────────────────────────────────────┌──────────┌──────────┌──────────首页    │───→│ 地图找电│───→│ 预约服务 └──────────└──────────└──────────┌──────────┌──────────┌──────────订单管理 │←───地址选择 │←───地址管理 └──────────└──────────└──────────┌──────────────────────────────────────────────高德地图服务                      ├──────────────────────────────────────────────瓦片服务 定位API POI搜索 地理编码     └──────────────────────────────────────────────└─────────────────────────────────────────────────────────────```

---

## 九、文件清| 文件 | 说明 | 状
|------|------| 属性 | 接口概览 | 接口名称 |------
| `pages/MapPage.tsx` | 地图找电工主页面 | 已实现
| `pages/user/profile/UserAddressEdit.tsx` | 地址编辑页面 | 已实现
| `components/user/UserShared.tsx` | 地址选择器组| 已实现
| `services/mapService.ts` | 地图服务（高德API| 📝 待开发
| `hooks/useGeolocation.ts` | 定位Hook | 📝 待开发
| `hooks/useAMap.ts` | 高德地图Hook | 📝 待开发
| `types/map.ts` | 地图类型定义 | 📝 待开发

---

## 十、开发优先级

### 10.1 Phase-1（核心功能）

| 功能 | 优先| 说明 
| 属性 | 接口概览 | 接口名称 |------| 属性 | 接口概览 | 接口名称 |--------| 属性 | 接口概览 | 接口名称 |------
| 地图找电| P0 | 核心功能，已实现基础版本 
| 地址选择 | P0 | 订单必需功能 
| 高德API集成 | P0 | 替换模拟数据 
| 定位服务 | P0 | 获取用户位置 
| POI搜索 | P1 | 地址搜索优化 

### 10.2 Phase-2（扩展功能）

| 功能 | 优先| 说明 
| 属性 | 接口概览 | 接口名称 |------| 属性 | 接口概览 | 接口名称 |--------| 属性 | 接口概览 | 接口名称 |------
| 路径规划 | P2 | 电工导航功能 
| 实时导航 | P2 | 集成导航SDK 
| 地图样式 | P2 | 深色模式
| 离线地图 | P3 | 弱网环境支持 
